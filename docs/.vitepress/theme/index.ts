import DefaultTheme from "vitepress/theme";
import type { Theme } from "vitepress";

// Extends the default VitePress theme. Light customization can be added here
// (custom components, layout slots, CSS) without diverging from upstream.
const theme: Theme = {
  ...DefaultTheme,
};

export default theme;
